/// AWS::SSO::Instance
///
/// Generated by rain build --pkl-class AWS::SSO::Instance
module aws.sso.instance

import "../../cloudformation.pkl"

/// The metadata that you apply to the Identity Center (SSO) Instance to help you categorize and organize them.
open class InstanceTag {
    Key: String(matches(Regex(#"[\w+=,.@-]+"#)))|Mapping
    Value: String(matches(Regex(#"[\w+=,.@-]+"#)))|Mapping
}

/// Resource Type definition for Identity Center (SSO) Instance
open class Instance extends cloudformation.Resource {

    Type = "AWS::SSO::Instance"


    /// The name you want to assign to this Identity Center (SSO) Instance
    hidden Name: (String(matches(Regex(#"^[\w+=,.@-]+$"#)))|Mapping)?

    /// 
    hidden Tags: (Listing<InstanceTag>)?

    Properties {
        ["Name"] = if (Name == null) null else Name
        ["Tags"] = if (Tags == null) null else Tags
    }

}
