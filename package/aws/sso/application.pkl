/// AWS::SSO::Application
///
/// Generated by rain build --pkl-class AWS::SSO::Application
module aws.sso.application

import "../../cloudformation.pkl"

typealias PortalOptionsConfigurationVisibility = "ENABLED"|"DISABLED"

/// A structure that describes the options for the access portal associated with an application
open class ApplicationPortalOptionsConfiguration {
    SignInOptions: (ApplicationSignInOptions)?
    Visibility: (PortalOptionsConfigurationVisibility|Mapping)?
}

typealias SignInOptionsOrigin = "IDENTITY_CENTER"|"APPLICATION"

/// A structure that describes the sign-in options for an application portal
open class ApplicationSignInOptions {
    ApplicationUrl: (String(matches(Regex(#"^http(s)?:\/\/[-a-zA-Z0-9+&@#\/%?=~_|!:,.;]*[-a-zA-Z0-9+&bb@#\/%?=~_|]$"#)))|Mapping)?
    Origin: SignInOptionsOrigin|Mapping
}

/// The metadata that you apply to the Identity Center (SSO) Application to help you categorize and organize them.
open class ApplicationTag {
    Key: String(matches(Regex(#"^[\w+=,.@-]+$"#)))|Mapping
    Value: String(matches(Regex(#"^[\w+=,.@-]+$"#)))|Mapping
}

/// Resource Type definition for Identity Center (SSO) Application
open class Application extends cloudformation.Resource {

    Type = "AWS::SSO::Application"


    /// The ARN of the application provider under which the operation will run
    hidden ApplicationProviderArn: String(matches(Regex(#"^arn:(aws|aws-us-gov|aws-cn|aws-iso|aws-iso-b):sso::aws:applicationProvider/[a-zA-Z0-9-/]+$"#)))|Mapping

    /// The description information for the Identity Center (SSO) Application
    hidden Description: (String|Mapping)?

    /// The ARN of the instance of IAM Identity Center under which the operation will run
    hidden InstanceArn: String(matches(Regex(#"^arn:(aws|aws-us-gov|aws-cn|aws-iso|aws-iso-b):sso:::instance/(sso)?ins-[a-zA-Z0-9-.]{16}$"#)))|Mapping

    /// The name you want to assign to this Identity Center (SSO) Application
    hidden Name: String(matches(Regex(#"^[\w+=,.@-]+$"#)))|Mapping

    /// A structure that describes the options for the portal associated with an application
    hidden PortalOptions: (ApplicationPortalOptionsConfiguration)?

    /// Specifies whether the application is enabled or disabled
    hidden Status: (ApplicationStatus|Mapping)?

    /// 
    hidden Tags: (Listing<ApplicationTag>)?

    Properties {
        ["ApplicationProviderArn"] = if (ApplicationProviderArn == null) null else ApplicationProviderArn
        ["Description"] = if (Description == null) null else Description
        ["InstanceArn"] = if (InstanceArn == null) null else InstanceArn
        ["Name"] = if (Name == null) null else Name
        ["PortalOptions"] = if (PortalOptions == null) null else PortalOptions
        ["Status"] = if (Status == null) null else Status
        ["Tags"] = if (Tags == null) null else Tags
    }

}


typealias ApplicationStatus = "ENABLED"|"DISABLED"
