/// AWS::M2::Deployment
///
/// Generated by rain build --pkl-class AWS::M2::Deployment
module aws.m2.deployment

import "../../cloudformation.pkl"

/// Represents a deployment resource of an AWS Mainframe Modernization (M2) application to a specified environment
open class Deployment extends cloudformation.Resource {

    Type = "AWS::M2::Deployment"


    /// The application ID.
    hidden ApplicationId: String(matches(Regex(#"^\S{1,80}$"#)))|Mapping

    /// The version number of the application to deploy
    hidden ApplicationVersion: Int|Mapping

    /// The environment ID.
    hidden EnvironmentId: String(matches(Regex(#"^\S{1,80}$"#)))|Mapping

    Properties {
        ["ApplicationId"] = if (ApplicationId == null) null else ApplicationId
        ["ApplicationVersion"] = if (ApplicationVersion == null) null else ApplicationVersion
        ["EnvironmentId"] = if (EnvironmentId == null) null else EnvironmentId
    }

}
