/// AWS::CloudFormation::GuardHook
///
/// Generated by rain build --pkl-class AWS::CloudFormation::GuardHook
module aws.cloudformation.guardhook

import "../../cloudformation.pkl"

typealias GuardHookAction = String|Mapping


/// Hook targets are the destination where hooks will be invoked against.
open class GuardHookHookTarget {
    Action: GuardHookAction
    InvocationPoint: GuardHookInvocationPoint
    TargetName: GuardHookTargetName
}

typealias GuardHookInvocationPoint = String|Mapping


typealias GuardHookRole = String|Mapping


/// S3 Source Location for the Guard files.
open class GuardHookS3Location {
    Uri: String|Mapping
    VersionId: (String|Mapping)?
}

typealias GuardHookStackName = String|Mapping


typealias GuardHookTargetName = String|Mapping


typealias GuardHookTargetOperation = String|Mapping


/// This is a CloudFormation resource for activating the first-party AWS::Hooks::GuardHook.
open class GuardHook extends cloudformation.Resource {

    Type = "AWS::CloudFormation::GuardHook"


    /// The typename alias for the hook.
    hidden Alias: String(matches(Regex(#"^(?!(?i)aws)[A-Za-z0-9]{2,64}::[A-Za-z0-9]{2,64}::[A-Za-z0-9]{2,64}$"#)))|Mapping

    /// The execution role ARN assumed by hooks to read Guard rules from S3 and write Guard outputs to S3.
    hidden ExecutionRole: GuardHookRole

    /// Attribute to specify CloudFormation behavior on hook failure.
    hidden FailureMode: GuardHookFailureMode|Mapping

    /// Attribute to specify which stacks this hook applies to or should get invoked for
    hidden HookStatus: GuardHookHookStatus|Mapping

    /// S3 Bucket where the guard validate report will be uploaded to
    hidden LogBucket: (String|Mapping)?

    /// 
    hidden Options: (Dynamic|Mapping)?

    /// 
    hidden RuleLocation: GuardHookS3Location

    /// Filters to allow hooks to target specific stack attributes
    hidden StackFilters: (Dynamic)?

    /// Attribute to specify which targets should invoke the hook
    hidden TargetFilters: (Dynamic)?

    /// Which operations should this Hook run against? Resource changes, stacks or change sets.
    hidden TargetOperations: Listing<GuardHookTargetOperation>

    Properties {
        ["Alias"] = if (Alias == null) null else Alias
        ["ExecutionRole"] = if (ExecutionRole == null) null else ExecutionRole
        ["FailureMode"] = if (FailureMode == null) null else FailureMode
        ["HookStatus"] = if (HookStatus == null) null else HookStatus
        ["LogBucket"] = if (LogBucket == null) null else LogBucket
        ["Options"] = if (Options == null) null else Options
        ["RuleLocation"] = if (RuleLocation == null) null else RuleLocation
        ["StackFilters"] = if (StackFilters == null) null else StackFilters
        ["TargetFilters"] = if (TargetFilters == null) null else TargetFilters
        ["TargetOperations"] = if (TargetOperations == null) null else TargetOperations
    }

}


typealias GuardHookFailureMode = "FAIL"|"WARN"

typealias GuardHookHookStatus = "ENABLED"|"DISABLED"
