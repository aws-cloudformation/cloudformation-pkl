/// AWS::Connect::AgentStatus
///
/// Generated by rain build --pkl-class AWS::Connect::AgentStatus
module aws.connect.agentstatus

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class AgentStatusTag {
    Key: String(matches(Regex(#"^(?!aws:)[a-zA-Z+-=._:/]+$"#)))|Mapping
    Value: String|Mapping
}

/// Resource Type definition for AWS::Connect::AgentStatus
open class AgentStatus extends cloudformation.Resource {

    Type = "AWS::Connect::AgentStatus"


    /// The description of the status.
    hidden Description: (String|Mapping)?

    /// The display order of the status.
    hidden DisplayOrder: (Int|Mapping)?

    /// The identifier of the Amazon Connect instance.
    hidden InstanceArn: String(matches(Regex(#"^arn:aws[-a-z0-9]*:connect:[-a-z0-9]*:[0-9]{12}:instance/[-a-zA-Z0-9]*$"#)))|Mapping

    /// The name of the status.
    hidden Name: String|Mapping

    /// A number indicating the reset order of the agent status.
    hidden ResetOrderNumber: (Boolean|Mapping)?

    /// The state of the status.
    hidden State: AgentStatusState|Mapping

    /// An array of key-value pairs to apply to this resource.
    hidden Tags: (Listing<AgentStatusTag>)?

    /// The type of agent status.
    hidden TypeProperty: (AgentStatusType|Mapping)?

    Properties {
        ["Description"] = if (Description == null) null else Description
        ["DisplayOrder"] = if (DisplayOrder == null) null else DisplayOrder
        ["InstanceArn"] = if (InstanceArn == null) null else InstanceArn
        ["Name"] = if (Name == null) null else Name
        ["ResetOrderNumber"] = if (ResetOrderNumber == null) null else ResetOrderNumber
        ["State"] = if (State == null) null else State
        ["Tags"] = if (Tags == null) null else Tags
        ["Type"] = if (TypeProperty == null) null else TypeProperty
    }

}


typealias AgentStatusState = "ENABLED"|"DISABLED"

typealias AgentStatusType = "ROUTABLE"|"CUSTOM"|"OFFLINE"
