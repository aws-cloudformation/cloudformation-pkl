/// AWS::S3Tables::TableBucket
///
/// Generated by rain build --pkl-class AWS::S3Tables::TableBucket
module aws.s3tables.tablebucket

import "../../cloudformation.pkl"

typealias TableBucketTableBucketARN = String|Mapping


typealias TableBucketTableBucketName = String|Mapping


typealias UnreferencedFileRemovalStatus = "Enabled"|"Disabled"

/// Settings governing the Unreferenced File Removal maintenance action. Unreferenced file removal identifies and deletes all objects that are not referenced by any table snapshots.
open class TableBucketUnreferencedFileRemoval {
    NoncurrentDays: (Int|Mapping)?
    Status: (UnreferencedFileRemovalStatus|Mapping)?
    UnreferencedDays: (Int|Mapping)?
}

/// Creates an Amazon S3 Tables table bucket in the same AWS Region where you create the AWS CloudFormation stack.
open class TableBucket extends cloudformation.Resource {

    Type = "AWS::S3Tables::TableBucket"


    /// 
    hidden TableBucketName: TableBucketTableBucketName

    /// 
    hidden UnreferencedFileRemoval: (TableBucketUnreferencedFileRemoval)?

    Properties {
        ["TableBucketName"] = if (TableBucketName == null) null else TableBucketName
        ["UnreferencedFileRemoval"] = if (UnreferencedFileRemoval == null) null else UnreferencedFileRemoval
    }

}
