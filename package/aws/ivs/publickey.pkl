/// AWS::IVS::PublicKey
///
/// Generated by rain build --pkl-class AWS::IVS::PublicKey
module aws.ivs.publickey

import "../../cloudformation.pkl"

/// 
open class PublicKeyTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Resource Type definition for AWS::IVS::PublicKey
open class PublicKey extends cloudformation.Resource {

    Type = "AWS::IVS::PublicKey"


    /// Name of the public key to be imported. The value does not need to be unique.
    hidden Name: (String(matches(Regex(#"^[a-zA-Z0-9-_]*$"#)))|Mapping)?

    /// The public portion of a customer-generated key pair.
    hidden PublicKeyMaterial: (String(matches(Regex(#"-----BEGIN PUBLIC KEY-----\r?\n([a-zA-Z0-9+/=\r\n]+)\r?\n-----END PUBLIC KEY-----(\r?\n)?"#)))|Mapping)?

    /// A list of key-value pairs that contain metadata for the asset model.
    hidden Tags: (Listing<PublicKeyTag>)?

    Properties {
        ["Name"] = if (Name == null) null else Name
        ["PublicKeyMaterial"] = if (PublicKeyMaterial == null) null else PublicKeyMaterial
        ["Tags"] = if (Tags == null) null else Tags
    }

}
