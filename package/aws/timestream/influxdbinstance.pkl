/// AWS::Timestream::InfluxDBInstance
///
/// Generated by rain build --pkl-class AWS::Timestream::InfluxDBInstance
module aws.timestream.influxdbinstance

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class InfluxDBInstanceTag {
    Key: String|Mapping
    Value: (String|Mapping)?
}

/// The AWS::Timestream::InfluxDBInstance resource creates an InfluxDB instance.
open class InfluxDBInstance extends cloudformation.Resource {

    Type = "AWS::Timestream::InfluxDBInstance"


    /// The allocated storage for the InfluxDB instance.
    hidden AllocatedStorage: (Int|Mapping)?

    /// The bucket for the InfluxDB instance.
    hidden Bucket: (String(matches(Regex(#"^[^_][^"]*$"#)))|Mapping)?

    /// The compute instance of the InfluxDB instance.
    hidden DbInstanceType: (InfluxDBInstanceDbInstanceType|Mapping)?

    /// The name of an existing InfluxDB parameter group.
    hidden DbParameterGroupIdentifier: (String(matches(Regex(#"^[a-zA-Z0-9]+$"#)))|Mapping)?

    /// The storage type of the InfluxDB instance.
    hidden DbStorageType: (InfluxDBInstanceDbStorageType|Mapping)?

    /// Deployment type of the InfluxDB Instance.
    hidden DeploymentType: (InfluxDBInstanceDeploymentType|Mapping)?

    /// Configuration for sending logs to customer account from the InfluxDB instance.
    hidden LogDeliveryConfiguration: (Dynamic)?

    /// The unique name that is associated with the InfluxDB instance.
    hidden Name: (String(matches(Regex(#"^[a-zA-z][a-zA-Z0-9]*(-[a-zA-Z0-9]+)*$"#)))|Mapping)?

    /// The organization for the InfluxDB instance.
    hidden Organization: (String|Mapping)?

    /// The password for the InfluxDB instance.
    hidden Password: (String(matches(Regex(#"^[a-zA-Z0-9]+$"#)))|Mapping)?

    /// Attach a public IP to the customer ENI.
    hidden PubliclyAccessible: (Boolean|Mapping)?

    /// An arbitrary set of tags (key-value pairs) for this DB instance.
    hidden Tags: (Listing<InfluxDBInstanceTag>)?

    /// The username for the InfluxDB instance.
    hidden Username: (String|Mapping)?

    /// A list of Amazon EC2 VPC security groups to associate with this InfluxDB instance.
    hidden VpcSecurityGroupIds: (Listing<String|Mapping>)?

    /// A list of EC2 subnet IDs for this InfluxDB instance.
    hidden VpcSubnetIds: (Listing<String|Mapping>)?

    Properties {
        ["AllocatedStorage"] = if (AllocatedStorage == null) null else AllocatedStorage
        ["Bucket"] = if (Bucket == null) null else Bucket
        ["DbInstanceType"] = if (DbInstanceType == null) null else DbInstanceType
        ["DbParameterGroupIdentifier"] = if (DbParameterGroupIdentifier == null) null else DbParameterGroupIdentifier
        ["DbStorageType"] = if (DbStorageType == null) null else DbStorageType
        ["DeploymentType"] = if (DeploymentType == null) null else DeploymentType
        ["LogDeliveryConfiguration"] = if (LogDeliveryConfiguration == null) null else LogDeliveryConfiguration
        ["Name"] = if (Name == null) null else Name
        ["Organization"] = if (Organization == null) null else Organization
        ["Password"] = if (Password == null) null else Password
        ["PubliclyAccessible"] = if (PubliclyAccessible == null) null else PubliclyAccessible
        ["Tags"] = if (Tags == null) null else Tags
        ["Username"] = if (Username == null) null else Username
        ["VpcSecurityGroupIds"] = if (VpcSecurityGroupIds == null) null else VpcSecurityGroupIds
        ["VpcSubnetIds"] = if (VpcSubnetIds == null) null else VpcSubnetIds
    }

}


typealias InfluxDBInstanceDbInstanceType = "db.influx.medium"|"db.influx.large"|"db.influx.xlarge"|"db.influx.2xlarge"|"db.influx.4xlarge"|"db.influx.8xlarge"|"db.influx.12xlarge"|"db.influx.16xlarge"

typealias InfluxDBInstanceDbStorageType = "InfluxIOIncludedT1"|"InfluxIOIncludedT2"|"InfluxIOIncludedT3"

typealias InfluxDBInstanceDeploymentType = "SINGLE_AZ"|"WITH_MULTIAZ_STANDBY"
