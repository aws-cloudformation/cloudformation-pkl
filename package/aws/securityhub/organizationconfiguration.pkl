/// AWS::SecurityHub::OrganizationConfiguration
///
/// Generated by rain build --pkl-class AWS::SecurityHub::OrganizationConfiguration
module aws.securityhub.organizationconfiguration

import "../../cloudformation.pkl"

/// The AWS::SecurityHub::OrganizationConfiguration resource represents the configuration of your organization in Security Hub. Only the Security Hub administrator account can create Organization Configuration resource in each region and can opt-in to Central Configuration only in the aggregation region of FindingAggregator.
open class OrganizationConfiguration extends cloudformation.Resource {

    Type = "AWS::SecurityHub::OrganizationConfiguration"


    /// Whether to automatically enable Security Hub in new member accounts when they join the organization.
    hidden AutoEnable: Boolean|Mapping

    /// Whether to automatically enable Security Hub default standards in new member accounts when they join the organization.
    hidden AutoEnableStandards: (OrganizationConfigurationAutoEnableStandards|Mapping)?

    /// Indicates whether the organization uses local or central configuration.
    hidden ConfigurationType: (OrganizationConfigurationConfigurationType|Mapping)?

    Properties {
        ["AutoEnable"] = if (AutoEnable == null) null else AutoEnable
        ["AutoEnableStandards"] = if (AutoEnableStandards == null) null else AutoEnableStandards
        ["ConfigurationType"] = if (ConfigurationType == null) null else ConfigurationType
    }

}


typealias OrganizationConfigurationAutoEnableStandards = "DEFAULT"|"NONE"

typealias OrganizationConfigurationConfigurationType = "CENTRAL"|"LOCAL"
