/// AWS::MediaLive::Network
///
/// Generated by rain build --pkl-class AWS::MediaLive::Network
module aws.medialive.network

import "../../cloudformation.pkl"

/// IP address cidr pool
open class NetworkIpPool {
    Cidr: (String|Mapping)?
}

typealias NetworkNetworkState = String|Mapping


/// 
open class NetworkRoute {
    Cidr: (String|Mapping)?
    Gateway: (String|Mapping)?
}

/// A key-value pair to associate with a resource.
open class NetworkTags {
    Key: (String|Mapping)?
    Value: (String|Mapping)?
}

/// Resource schema for AWS::MediaLive::Network.
open class Network extends cloudformation.Resource {

    Type = "AWS::MediaLive::Network"


    /// The list of IP address cidr pools for the network
    hidden IpPools: Listing<NetworkIpPool>

    /// The user-specified name of the Network to be created.
    hidden Name: String|Mapping

    /// The routes for the network
    hidden Routes: (Listing<NetworkRoute>)?

    /// A collection of key-value pairs.
    hidden Tags: (Listing<NetworkTags>)?

    Properties {
        ["IpPools"] = if (IpPools == null) null else IpPools
        ["Name"] = if (Name == null) null else Name
        ["Routes"] = if (Routes == null) null else Routes
        ["Tags"] = if (Tags == null) null else Tags
    }

}
