/// AWS::Bedrock::FlowAlias
///
/// Generated by rain build --pkl-class AWS::Bedrock::FlowAlias
module aws.bedrock.flowalias

import "../../cloudformation.pkl"

/// Details about the routing configuration for a Flow alias.
open class FlowAliasFlowAliasRoutingConfigurationListItem {
    FlowVersion: (String(matches(Regex(#"^(DRAFT|[0-9]{0,4}[1-9][0-9]{0,4})$"#)))|Mapping)?
}

typealias FlowAliasTagsMap = Mapping<String, Any>


/// Definition of AWS::Bedrock::FlowAlias Resource Type
open class FlowAlias extends cloudformation.Resource {

    Type = "AWS::Bedrock::FlowAlias"


    /// Description of the Resource.
    hidden Description: (String|Mapping)?

    /// Arn representation of the Flow
    hidden FlowArn: String(matches(Regex(#"^arn:aws(-[^:]+)?:bedrock:[a-z0-9-]{1,20}:[0-9]{12}:flow/[0-9a-zA-Z]{10}$"#)))|Mapping

    /// Name for a resource.
    hidden Name: String(matches(Regex(#"^([0-9a-zA-Z][_-]?){1,100}$"#)))|Mapping

    /// Routing configuration for a Flow alias.
    hidden RoutingConfiguration: Listing<FlowAliasFlowAliasRoutingConfigurationListItem>

    /// 
    hidden Tags: (FlowAliasTagsMap)?

    Properties {
        ["Description"] = if (Description == null) null else Description
        ["FlowArn"] = if (FlowArn == null) null else FlowArn
        ["Name"] = if (Name == null) null else Name
        ["RoutingConfiguration"] = if (RoutingConfiguration == null) null else RoutingConfiguration
        ["Tags"] = if (Tags == null) null else Tags
    }

}
