/// AWS::MemoryDB::MultiRegionCluster
///
/// Generated by rain build --pkl-class AWS::MemoryDB::MultiRegionCluster
module aws.memorydb.multiregioncluster

import "../../cloudformation.pkl"

/// A key-value pair to associate with a resource.
open class MultiRegionClusterTag {
    Key: String(matches(Regex(#"^(?!aws:)(?!memorydb:)[a-zA-Z0-9 _\.\/=+:\-@]{1,128}$"#)))|Mapping
    Value: String(matches(Regex(#"^(?!aws:)(?!memorydb:)[a-zA-Z0-9 _\.\/=+:\-@]{1,256}$"#)))|Mapping
}

/// The AWS::MemoryDB::Multi Region Cluster resource creates an Amazon MemoryDB Multi Region Cluster.
open class MultiRegionCluster extends cloudformation.Resource {

    Type = "AWS::MemoryDB::MultiRegionCluster"


    /// Description of the multi region cluster.
    hidden Description: (String|Mapping)?

    /// The engine type used by the multi region cluster.
    hidden Engine: (String|Mapping)?

    /// The Redis engine version used by the multi region cluster.
    hidden EngineVersion: (String|Mapping)?

    /// The name of the Multi Region cluster. This value must be unique as it also serves as the multi region cluster identifier.
    hidden MultiRegionClusterNameSuffix: (String(matches(Regex(#"[a-z][a-z0-9\-]*"#)))|Mapping)?

    /// The name of the parameter group associated with the multi region cluster.
    hidden MultiRegionParameterGroupName: (String|Mapping)?

    /// The compute and memory capacity of the nodes in the multi region cluster.
    hidden NodeType: String|Mapping

    /// The number of shards the multi region cluster will contain.
    hidden NumShards: (Int|Mapping)?

    /// A flag that enables in-transit encryption when set to true.
    ///
    /// 
    /// You cannot modify the value of TransitEncryptionEnabled after the cluster is created. To enable in-transit encryption on a cluster you must set TransitEncryptionEnabled to true when you create a cluster.
    hidden TLSEnabled: (Boolean|Mapping)?

    /// An array of key-value pairs to apply to this multi region cluster.
    hidden Tags: (Listing<MultiRegionClusterTag>)?

    /// An enum string value that determines the update strategy for scaling. Possible values are 'COORDINATED' and 'UNCOORDINATED'. Default is 'COORDINATED'.
    hidden UpdateStrategy: (MultiRegionClusterUpdateStrategy|Mapping)?

    Properties {
        ["Description"] = if (Description == null) null else Description
        ["Engine"] = if (Engine == null) null else Engine
        ["EngineVersion"] = if (EngineVersion == null) null else EngineVersion
        ["MultiRegionClusterNameSuffix"] = if (MultiRegionClusterNameSuffix == null) null else MultiRegionClusterNameSuffix
        ["MultiRegionParameterGroupName"] = if (MultiRegionParameterGroupName == null) null else MultiRegionParameterGroupName
        ["NodeType"] = if (NodeType == null) null else NodeType
        ["NumShards"] = if (NumShards == null) null else NumShards
        ["TLSEnabled"] = if (TLSEnabled == null) null else TLSEnabled
        ["Tags"] = if (Tags == null) null else Tags
        ["UpdateStrategy"] = if (UpdateStrategy == null) null else UpdateStrategy
    }

}


typealias MultiRegionClusterUpdateStrategy = "COORDINATED"|"UNCOORDINATED"
