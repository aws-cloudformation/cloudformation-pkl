/// AWS::QBusiness::Retriever
///
/// Generated by rain build --pkl-class AWS::QBusiness::Retriever
module aws.qbusiness.retriever

import "../../cloudformation.pkl"

/// 
open class RetrieverKendraIndexConfiguration {
    IndexId: String(matches(Regex(#"^[a-zA-Z0-9][a-zA-Z0-9-]{35}$"#)))|Mapping
}

/// 
open class RetrieverNativeIndexConfiguration {
    IndexId: String(matches(Regex(#"^[a-zA-Z0-9][a-zA-Z0-9-]{35}$"#)))|Mapping
}

typealias RetrieverRetrieverConfiguration = RetrieverRetrieverConfigurationNativeIndexConfiguration|RetrieverRetrieverConfigurationKendraIndexConfiguration


/// 
open class RetrieverRetrieverConfigurationKendraIndexConfiguration {
    KendraIndexConfiguration: RetrieverKendraIndexConfiguration
}

/// 
open class RetrieverRetrieverConfigurationNativeIndexConfiguration {
    NativeIndexConfiguration: RetrieverNativeIndexConfiguration
}

typealias RetrieverRetrieverStatus = String|Mapping


typealias RetrieverRetrieverType = String|Mapping


/// 
open class RetrieverTag {
    Key: String|Mapping
    Value: String|Mapping
}

/// Definition of AWS::QBusiness::Retriever Resource Type
open class Retriever extends cloudformation.Resource {

    Type = "AWS::QBusiness::Retriever"


    /// 
    hidden ApplicationId: String(matches(Regex(#"^[a-zA-Z0-9][a-zA-Z0-9-]{35}$"#)))|Mapping

    /// 
    hidden Configuration: RetrieverRetrieverConfiguration

    /// 
    hidden DisplayName: String(matches(Regex(#"^[a-zA-Z0-9][a-zA-Z0-9_-]*$"#)))|Mapping

    /// 
    hidden RoleArn: (String(matches(Regex(#"^arn:[a-z0-9-\.]{1,63}:[a-z0-9-\.]{0,63}:[a-z0-9-\.]{0,63}:[a-z0-9-\.]{0,63}:[^/].{0,1023}$"#)))|Mapping)?

    /// 
    hidden Tags: (Listing<RetrieverTag>)?

    /// 
    hidden TypeProperty: RetrieverRetrieverType

    Properties {
        ["ApplicationId"] = if (ApplicationId == null) null else ApplicationId
        ["Configuration"] = if (Configuration == null) null else Configuration
        ["DisplayName"] = if (DisplayName == null) null else DisplayName
        ["RoleArn"] = if (RoleArn == null) null else RoleArn
        ["Tags"] = if (Tags == null) null else Tags
        ["Type"] = if (TypeProperty == null) null else TypeProperty
    }

}
